# dotfiles

Personal **dotfiles** collection. Still figuring out the best way to manage them — considering options like `stow`, `chezmoi`, `nix`, etc. 🤔

Background Context:
  * Primary setup runs **CachyOS** with **Hyprland**.
  * Uses **DMS**([DankMaterialShell](https://github.com/AvengeMedia/DankMaterialShell)) to manage the bar, widgets, and various utilities (greeter, clipboard manager, etc.).
  * Configuration for GUI applications is not currently included.
